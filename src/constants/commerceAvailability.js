// Public sales are intentionally paused while prices, products, and the
// fulfilment flow are prepared. Keep the UI aligned with the server guards.
export const PUBLIC_SELF_SERVICE_BILLING_ENABLED = false;
export const PUBLIC_MATERIAL_SALES_ENABLED = false;
