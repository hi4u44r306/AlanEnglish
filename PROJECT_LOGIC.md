# Alan English 專案邏輯

最後核對日期：2026-09-03

本文件是 Alan English 目前產品行為、角色、權限、帳號生命週期與跨功能不變條件的單一參考文件。開發新功能或調整頁面顯示前，應先確認修改沒有違反本文件。

## 文件分工

- `PROJECT_LOGIC.md`：記錄目前應成立的產品與系統邏輯。
- `AGENTS.md`：記錄開發、安全、Git、部署與驗收規範。
- `docs/PROJECT_STATUS.md`：記錄 commit、PR、部署版本、進行中工作與測試狀態。
- `docs/網站使用手冊.md`：記錄學生、老師與管理員實際操作方式，也是圖片、PDF 與教學影片的文字母檔；功能、權限、價格或流程異動時必須同步更新。
- Migration 與 Edge Function：是資料庫及後端實際執行邏輯；若與本文件不一致，必須先確認正式環境現況，再以 additive migration 或程式修改統一，不得只改文件掩蓋差異。

本文件不記錄金鑰、密碼、學生個資、一次性驗證資料或其他 Secret。

## 1. 系統責任邊界

### 1.1 前端

前端使用 React、Redux、React Router 與 SCSS，負責：

- 顯示符合角色與有效功能權限的入口。
- 引導登入、註冊、邀請、付款與學習流程。
- 收集使用者操作資料並呼叫受保護的後端。
- 在手機、桌面與 iPhone safe area 下維持可操作的版面。

前端隱藏按鈕只改善使用體驗，不構成安全授權。直接輸入網址、修改請求內容或偽造前端狀態，都不能取得額外權限。

### 1.2 Firebase Authentication

Firebase Authentication 是聽力與學習平台的唯一登入身分來源，負責 Email／密碼登入、英文班帳號對應、Email 驗證、密碼重設與登入 Session。獨立教材商城帳號是下節明定的例外。

- 不改用 Supabase Auth。
- Firebase UID 必須對應 Supabase `students` 資料。
- 一般會員、網購者、試用者、老師與管理員使用真實 Email 登入。
- 新建立的英文班在校生使用唯一 `login_username` 與學生自行設定的密碼登入；密碼至少 6 個字元，Firebase 內部仍使用系統識別 Email，但不得顯示給學生或當成可收信 Email。
- 除非使用者主動登出、帳號被停用或 Session 失效，重新開啟網站後應保持登入。
- 前端呼叫受保護的 Edge Function 時必須附上有效 Firebase ID Token。
- Firebase 專案只使用 Authentication；前端不得初始化 Firestore、Realtime Database 或 Firebase Storage，資料、檔案與學習紀錄分別由 Supabase 與 Cloudflare R2 負責。

### 1.2.1 獨立教材商城帳號例外

- 實體教材商城使用獨立的 Supabase Auth Email／密碼帳號；不得讀取、取代或同步聽力平台的 Firebase Session。
- 同一個 Email 可以分別註冊商城與聽力平台帳號，兩邊的密碼、Email 驗證與登入狀態彼此獨立。
- 商城帳號只用於購物車結帳、收件資料、付款訂單及物流查詢，不因此登入或建立聽力平台的 Firebase Session。
- 已付款且尚未領取的商城訂單，可在使用者另外建立並驗證同一 Email 的 Firebase 學習帳號後，由後端一次性認領；後端必須重新確認訂單已付款、Email 相符且尚未被其他學生認領，才建立三本永久 entitlement 與 90 天 grant。不能只因兩邊輸入相同 Email 就共享密碼、Session 或未付款訂單。
- 商城客戶資料只能由驗證商城 access token 的 Edge Function 依 `auth.users.id` 讀寫；商城管理操作仍使用 Firebase 管理員身分。

### 1.3 Supabase

Supabase PostgreSQL、RLS 與 Edge Functions 負責資料、授權、學習紀錄、額度與管理操作。

- Edge Function 必須驗證 Firebase ID Token。
- 後端必須依 UID 重新讀取呼叫者的角色、帳號狀態、學生類型、班級及有效權限。
- 不得信任前端傳入的 `role`、`student_id`、`learner_type`、班級或方案狀態。
- 公開 schema 的資料表應啟用 RLS；可由 Data API 存取時，仍需明確的資料列授權。
- `service_role` 只能存在 Edge Function Secret，不得進入 React、Git、Console、錯誤訊息或文件。
- 資料庫變更只能建立新的 additive migration，不得修改已部署 migration 或重設正式資料庫。

### 1.4 Cloudflare R2

- 正式教材音檔保持私有。
- 前端透過短效預簽網址播放。
- 必須保留 HTTP Range Request、時間軸、續播與分段載入。
- 在來源與 R2 資料完整性確認前，不刪除備份音檔。

## 2. 身分、帳號與學習資格

系統必須分開理解以下狀態，不能混為一個欄位：

| 狀態 | 回答的問題 | 主要值或來源 |
| --- | --- | --- |
| 角色 | 使用者能管理什麼 | `student`、`teacher`、`admin` |
| 帳號狀態 | 帳號能否登入及呼叫系統 | `active`、`archived` |
| 開通狀態 | 邀請／Firebase 註冊流程是否完成 | 待開通、等待 Email 驗證、已開通、過期、撤銷等 |
| 學生類型 | 學習者屬於哪種商業關係 | `academy_student`、`textbook_customer`、`trial_user` |
| 在學狀態 | 英文班學生是否仍在校 | `active` 或非在學狀態 |
| 方案／授權 | 哪些權限來源仍有效 | subscription 與 access grants |
| 功能權限 | 此刻能否使用單一功能 | `effective_access.features.*` |

### 2.1 角色

- `student`：只能存取自己的教材、學習紀錄、作業、額度與獎勵。
- `teacher`：只能管理授權班級與相應學生資料；不可因前端顯示而取得管理員權限。
- `admin`：可管理全班級、帳號、會員、目錄、獎勵與系統頁面。
- 禁止再使用 `class === "Teacher"` 之類的舊班級判斷角色。

### 2.2 學生類型

- `academy_student`：英文班學生。
- `textbook_customer`：購買教材的網路使用者。
- `trial_user`：七天試用者。

`trial_user` 只代表尚未付費的七天試用關係。基本自主學習會員完成付款並啟用，或教材購買確認為已付款後，後端必須把該學生類型轉為 `textbook_customer`；不得只新增方案或權限而留下「七天試用會員」身分。轉換只能套用於原本的 `trial_user`，不得覆蓋 `academy_student` 或其在校／離校歷史。

英文班班級固定為 E1、E3、E5、E7。升班、留班或降級由老師或管理員決定，不可依年份自動改班，也不可建立 E2、E4、E6、E8 作為年度升班結果。

### 2.3 停用帳號

當 `account_status = archived`：

- 帳號預設不顯示在管理清單，但管理員可用「已停用」篩選找到。
- 管理頁以紅底／紅框明確標示，操作以「恢復」為主。
- 即使原有訂閱、AI 加購或教材授權尚未到期，也不得登入或使用功能。
- Edge Function 回傳 `ACCOUNT_ARCHIVED` 時，前端清除快取並登出。
- 歷史學習、作業、點數、兌換與購買資料保留。
- 恢復後才重新依當下有效授權計算可用功能；恢復帳號不代表自動恢復已到期方案。

## 3. 疊加式有效權限

所有功能權限以後端計算的 `effective_access` 為準。它至少包含：

- `student_id`
- `role`
- `learner_type`
- `is_active`
- `effective_access_end`
- `days_remaining`
- `ai_daily_limit`
- `plan_codes`
- `features`
- `grants`

目前功能旗標包括：

- `listening`
- `ai_materials`
- `conversation`
- `assignments`
- `review`
- `requires_book_entitlement`

### 3.1 疊加原則

以下來源可以同時存在，且不得互相覆蓋：

- 英文班在學權限
- 教材購買權限
- 七天試用權限
- 一般會員訂閱
- 離校學生訂閱
- AI 教材與發音練習加購
- 管理員手動贈送
- 開通碼兌換權限

例如英文班學生另外購買教材或 AI 加購時，只新增相應 grant；退班後，已購教材與仍有效的獨立加購不能消失。

英文班學生每一段 enrollment 期間，與該班級教材版本生效區間有重疊的教材，都屬於該生實際適用的歷史教材。為相容早於教材版本表的舊資料，盤點還要補入 enrollment 期間班級作業所用教材，以及同期間具有伺服器播放進度的教材。執行離校時，後端必須在同一個資料庫交易內，將學生所有歷史 enrollment 的這些教材建立為永久 `student_book_entitlements`（來源為 `academy_history`），再結束 enrollment 並記錄離校事件。既有離校生依同一規則回填，且處理必須可重複執行而不產生重複權限。

### 3.2 到期原則

- 權限到期後鎖定相應內容，但不刪除學習進度。
- 會員中心對已到期方案應顯示會員紀錄中的實際結束日；若歷史資料沒有可用日期，顯示「已結束」，不得顯示「無期限」。
- 不能只用 `class = null` 判斷網購者，也不能只看單一舊版 `plan` 欄位。
- 是否能使用功能應看 `features`；只有 `requires_book_entitlement = true` 的權限來源才需再逐本檢查教材 entitlement。基本會員與在校方案可直接使用全部正式聽力教材。
- 頁面可以先依 `features` 決定顯示，Edge Function 仍須在每次資料讀寫時重新授權。

## 4. 商業方案邏輯

| 類型／方案 | 核心權限 | 作業 | AI 教材生成 | 目前價格或額度 |
| --- | --- | --- | --- | --- |
| 七天試用 | 引導式試用內容，不假設擁有實體教材 | 無 | 依試用方案 | 目前實作為 7 天內總共 7 次、每日 2 次 |
| 網路教材購買者 | 購買的三本實體教材擁有權永久保留；90 天有效期間可使用全部正式聽力教材與基本學習功能 | 無 | 不因購書自動取得 | 購書後附贈 90 天網站使用權且不自動續費；到期可選基本會員 NT$299／月 |
| 基本自主學習會員 | 全部已啟用的正式聽力教材、情境會話與智慧複習；不包含實體教材 | 無 | 不因基本會員自動取得 | NT$299／月 |
| 英文班在校生 | 全部已啟用的正式聽力教材、AI 教材、發音練習、會話、複習與所屬班級作業 | 有，限所屬班級 | 有，每日 5 次、每月 150 次 | 英文班月費目前 NT$2,800，已包含平台全部學習功能 |
| 英文班離校生 | 永久保留所有歷史教材與紀錄；有效基本會員可使用全部正式聽力教材 | 無新作業 | 需獨立加購 | 基本會員 NT$299／月；AI／發音目標另加 NT$299／月 |
| AI 教材與發音練習 | 增加獨立 `ai_materials` 與 `pronunciation` 權限 | 不改變作業資格 | 有 | 目標 NT$299／月，每日 5 次、每月 150 次；非在校生須搭配有效基本會員，合計目標 NT$598／月 |
| 老師／管理員 | 依工作角色使用管理與教材功能 | 依角色 | 可使用 | 不以學生加購判斷 |

NT$299 基本會員目前使用 Stripe 沙盒 Price。AI「教材與發音練習」已確認目標價為 NT$299／月，但既有 Stripe Price 仍為 NT$499；新的不可變 Stripe Price、方案資料、付款頁、會員頁、公開方案、FAQ 與條款必須同批切換，切換前不得先顯示 NT$299，以免顯示金額與實際扣款不一致。AI 現行新訂閱只使用 `ai_materials_addon_monthly`；`ai_materials_general_monthly` 僅保留為停用的歷史代碼，不得再公開、販售或手動授權。三本組合教材包單一售價目前為 NT$1,380，不另設會員教材價；商品必須補齊課本、Workbook 與聽力本後才可重新上架。正式 Stripe 商品與 Price 尚未建立前，網站必須明示為測試付款，不得宣稱正式收款已啟用。

`AI Premium` 是同時具備 AI 教材與發音練習權限的前端稱號／徽章，不是另一個商品、方案代碼或價格。有效在校英文班學生可由 `academy_internal` 取得；其他學生由有效 `ai_materials_addon_monthly` 取得。歷史 `ai_materials_general_monthly` 紀錄只供相容舊帳務。商品、付款與帳務名稱固定為「AI 教材與發音練習」。

任何價格調整都必須同步更新公開首頁、方案比較、商品／方案資料、Stripe 商品或價格、會員頁、FAQ 與會員條款，不可只改畫面文字。

### 4.1 教材擁有權、網站使用權與商品包

- 教材擁有權與網站使用權是兩個 ledger。購買、自購開通碼或管理員贈送的教材不得因方案到期、轉班或離校而刪除。
- 一般教材包購買成功後，新增永久教材 entitlement，並新增一筆 90 天、`auto_renews = false` 的網站 access grant。
- 所有顧客使用商品包的單一售價；購買成功都新增三本永久教材 entitlement 及 90 天、不自動續費的平台 grant，不因當下是否已有基本會員而減少權益。
- NT$299 基本會員可使用全部已啟用且 `content_scope = formal` 的聽力教材，並包含會話與複習；不包含實體教材、試用／展示內容、AI Premium 或英文班作業。
- 離校生不需要重新購買以前上課使用的實體教材；訂閱 NT$299 後與其他基本會員相同，可使用全部正式聽力教材，但仍不得取得新班級作業。
- 教材包附贈的 90 天平台權限與 NT$299 基本會員相同，可使用全部已啟用正式聽力教材；實體書擁有權仍只記錄實際購買的三本。開通碼或管理員指定為 `requires_book_entitlement = true` 的特殊權限來源才逐本限制。帳號、永久教材擁有權及歷史進度在平台方案到期時保留，不得刪除。
- 商品包與班級教材設定分開。商品包上架前必須各有一本課本、一本 Workbook、一本聽力本、單一售價、Stripe 測試 Product 與單一 Price 對應，並固定附贈 90 天。
- 分級測驗推薦只使用 `status = published` 且 `stripe_livemode = false` 的教材包；沒有符合條件的教材包時不顯示推薦。只有一至兩組商品時只顯示一個最接近程度的「建議」結果，不得重複同一商品假裝成三種難度；三組以上才依 `recommendation_rank` 顯示較簡單、建議與較有挑戰。
- AI 教材與發音練習方案獨立加購，不得自行包入教材商品包。目前不建立年費方案。

### 4.2 歷史方案相容規則

- `all_access_monthly`、`listening_monthly`、`students.plan = allcover` 與 `students.plan = listeningonly` 都是舊版資料，只能用於辨識及轉換歷史帳號。
- 現行介面不得顯示舊商品名稱；尚未完成資料轉換時統一顯示「歷史會員權限（待轉換）」。
- 管理員不得再修改、重新公開、產生啟用碼或贈送上述舊方案。
- 會員狀態、方案名稱與功能入口必須以 `effective_access` 及有效 grant 為準，不得回退使用舊 `students.plan` 或失效的 `memberships.plan_id`。
- 試用額度已統一為七天內共七次、每日兩次；`AGENTS.md`、公開頁面與後端限制必須維持一致。

### 4.3 實體教材商城訂單

- `/shop` 對所有人公開瀏覽；加入購物車不需登入，結帳與歷史訂單需使用獨立商城帳號。
- 商品、數量、庫存、售價與運費一律由後端重新查詢，不信任購物車保存的價格。
- 商城固定使用商品包的一般售價；聽力平台會員價與 Firebase 學生身分不會自動套用到獨立商城。
- 建立 Stripe Checkout 前由資料庫原子保留有限庫存；建立失敗、付款失敗或 Checkout 逾時要釋放保留庫存。
- 顧客從 Stripe 返回取消付款時，後端必須先使原 Checkout Session 失效，再釋放保留庫存並將訂單標記為取消、不出貨；歷史訂單應以明顯紅色顯示，仍在等待付款的訂單也要提供安全的取消入口。
- 只有通過 Stripe webhook 簽章、訂單所有權、測試／正式模式、幣別與金額核對後，付款狀態才能變成 `paid`。
- 付款狀態與出貨狀態分開。未付款是 `awaiting_payment`；確認收款後才是 `preparing`，之後只能依序前進為 `shipping`、`completed`，不能由前端自行改變。
- 訂單保存購買當下的商品名稱、單價、數量、運費與收件地址快照；日後修改商品不改寫歷史訂單。
- 管理員填寫物流公司與單號後才可設為運送中；客戶只能查看自己的訂單與物流資料。
- 商城目前只支援台灣地址與 TWD。正式啟用前必須確認運費、退換貨／隱私條款、Supabase Email 驗證 redirect、Stripe webhook 事件與測試付款流程。

## 5. 路由、頁面與入口顯示

### 5.1 路由保護

- `ProtectedRoute` 先驗證登入，再檢查 `allowedRoles`。
- 標記 `requiresActiveMembership` 的學生頁會將無有效學習權限的學生導向會員中心。
- 角色可進入路由不代表擁有頁內所有功能；功能仍看 `effective_access.features`，資料仍由後端授權。
- 老師／管理員工作頁不以學生會員到期判斷。

### 5.2 主要學生入口

| 入口 | 顯示或可用條件 |
| --- | --- |
| Dashboard | 學生已登入且帳號可用；內容依有效權限裁切 |
| 今日／班級作業 | `features.assignments = true`，且為有效在學英文班學生 |
| 教材聽力 | `features.listening = true`；若 `requires_book_entitlement = true` 再檢查逐本 entitlement，否則可使用全部正式聽力教材 |
| AI 教材 | 老師／管理員，或學生 `features.ai_materials = true` |
| AI 加購宣傳 | 學生 `features.ai_materials = false`；已有任何有效 AI 權限時不重複推銷 |
| 智慧錯題複習 | `features.review = true` |
| 英文會話 | `features.conversation = true` |
| 會員中心 | 學生登入後可查看，即使學習方案失效仍可進入處理方案 |
| 排行榜／獎品商城 | XP 排行依後端規則顯示；AE Points 取得與獎品商城只限有效在校英文班學生 |

學生沒有 AI 權限卻直接進入 AI 教材路由時，頁面必須停在頂部顯示加購卡，不能自動捲過銷售內容；生成工作區必須隱藏且後端拒絕生成。

### 5.3 導覽列與浮動入口

- 網購者、試用者及離校生不得看到英文班作業入口。
- AI 無權限學生可看到「AI 教材與發音方案」或加購入口，但不能看到可直接生成的假象。
- `AssignmentShortcut` 依 `features.assignments` 顯示，不能只看角色或班級文字。
- Navbar、Sidebar、MusicPlayer、今日作業與發布作業浮動按鈕不得互相遮擋。
- 教材商城導覽以「逛教材、購物車、我的訂單、商城帳號」為第一層；聽力學習平台與公開網站屬於其他服務，不得與商城核心操作混在同一層造成誤解。
- 商城手機版 Header 只保留品牌、購物車與選單；選單依「商城功能、商城帳號、其他服務」分組並顯示文字說明，不能只留下用途不明的圖示。
- 商城導覽必須依目前路由反白；手機選單開啟時鎖定背景捲動，並可用關閉按鈕、遮罩或 Escape 關閉。

## 6. AI 教材

### 6.1 學生授權

- 一般學生沒有 `features.ai_materials` 時不可生成 AI 教材。
- 有效在校英文班學生由 `academy_internal` 直接取得 AI 教材與發音練習，不需要另外購買 AI 加購。
- 購買教材、90 天平台使用權或基本會員不會自動贈送 AI 教材與發音練習。
- 學生可透過有效的 AI 加購或試用方案取得權限。
- 一般會員與英文班離校生的「AI 教材與發音練習」目標 NT$299 必須搭配有效的 NT$299 基本會員，合計目標 NT$598／月；正式切換前仍依既有 Stripe NT$499 Price 收費與顯示。
- 發音練習使用獨立 `features.pronunciation` 判斷，暫時相容既有 `pronunciation_practice` 與 AI 方案資料，不再只依 `ai_materials` 猜測權限。
- 老師與管理員保留工作用途的 AI 教材權限。

### 6.2 額度

- 有效在校英文班學生與 AI 教材／發音加購：個人 AI 教材每日 5 次、台灣時間每月 150 次；發音練習沿用相應方案權限。
- 試用：目前每日 2 次、七天內總共 7 次。
- 每月 150 次限制套用有效在校英文班方案與現行 `ai_materials_addon_monthly`；歷史 `ai_materials_general_monthly` 僅供舊資料相容，不可再建立新訂閱，也不可誤傷試用方案或工作角色。
- 只有成功生成教材才消耗額度；驗證失敗、後端錯誤或未產生結果不應扣次數。
- 額度與重設時間由後端計算，前端只顯示結果。

### 6.3 AI 作業與成本規則

- 混合作業 V2 可包含 AI 選擇題，但只提供給符合班級資格的有效在校英文班學生，不看個人的 AI 加購狀態。
- AI 題組由老師根據已核准教材文字建立、預覽及發布一次；學生使用同一份不可變題目快照，重做不重新生成，也不消耗學生個人 AI 額度。
- 只有頁碼而沒有已發布 `source_text`／逐字稿時，後端必須拒絕生成，不得根據書名與頁碼猜題。
- 既有 AI 類作業與成績保留相容；未開啟 `assignment_v2` 的學生繼續使用既有聽力作業流程。

## 7. 英文班作業

### 7.1 學生資格

作業只屬於有效在學且班級相符的英文班學生：

```text
learner_type = academy_student
enrollment_status = active
student_class = assignment_target_class
features.assignments = true
```

- 目標班級必須是 E1、E3、E5 或 E7。
- 空白班級不可代表「全部學生」。
- 網購者、試用者及離校生不得讀取新作業。
- 轉班後只讀取新班級作業，不因舊班級歷史而繼續收到新作業。
- 老師只能處理授權班級；管理員可以處理所有班級。

### 7.2 發布內容

- 舊版作業維持純聽力並使用 `schema_version = 1`；不得為導入新版而改寫或刪除既有作業與進度。
- 混合作業 V2 使用 `schema_version = 2`，一份作業可依序加入聽力、AI 選擇題、發音練習或三者組合。
- 聽力可從一本以上教材選擇多個音檔，每檔設定 1～10 次有效聆聽，預設 3 次。
- AI 選擇題由老師選教材與頁碼範圍，使用教師核准來源生成一次，老師預覽後才保存題目快照及發布。
- 發音練習由老師選教材與頁碼範圍及提示句；預設每句完成一次有效評分，也可指定目標分數，每句最多 3 次計分，後端只保存分數結果、不保存原始錄音。
- 老師先在 `book_page_learning_content` 儲存草稿或發布經核對的頁面文字與發音提示句；修改內容會建立新版本，既有作業仍使用原本的不可變快照。只有 `published` 且具文字來源的頁面可加入 AI 或發音活動。
- V2 發布前必須由 Edge Function 重新核對老師授權班級、目前班級教材、音檔歸屬、AI 題組擁有權、頁面文字與提示句；前端傳入的班級、學生、頁碼、題目及分數不可直接信任。
- 學生 V2 作答、逐項進度與完成獎勵尚未發布前，不得把 V2 作業推送給學生或對真實班級啟用 `assignment_v2` 灰度旗標。
- 介面依活動與教材分組顯示頁碼／Unit、題數、發音句數、完成條件及順序，所有必做項目達標後才算整份完成。
- V2 先由 `assignment_v2` 學生灰度旗標控制，完成權限、冪等、截止時間、手機與成本驗收後才分批開放。

### 7.3 停用作業

介面上的「刪除功課」採安全停用／封存：

- 只有建立者或管理員可操作。
- 停用後學生不再看到該作業。
- 已有作答、聆聽與完成進度保留。
- 不以硬刪除破壞歷史報表。

### 7.4 完成與獎勵

Dashboard 以「完成老師作業可獲得 30 XP；有效在校生另得 5 AE Points」作為學習提示。新規則必須同時滿足：

- 一次通過 80% 後端驗證的有效聽力，會累加一般總聽力紀錄。
- 若該音檔屬於目前有效作業，而且聽力工作階段發生在作業發布後、截止前，同一次聽力也會使該作業進度 +1。
- 作業發布前的終身播放次數不得回頭計入新作業。
- V3（2026-09-03 確認，尚未部署）：同一音檔同時出現於多份有效作業時，每次 session 只推進一份尚未達到該音檔指定次數的作業，依截止日最早優先（無截止日最後，再依建立時間、ID）。不論從教材頁或作業頁進入，後端都使用相同規則，並重新確認有效在校／班級資格。
- 同一次聆聽只計作業或自主熟練其中之一；總次數仍累計。該音檔所有有效作業要求都達標後，後續播放才計入自主 10 次，其他尚未完成音檔不會卡住此檔的自主練習。
- 自主熟練已領過的音檔，日後仍可完成新發布的作業、領取該份作業獎勵，但不能再領此檔自主熟練獎勵。
- 同一作業只能發獎一次。
- 重播或重送完成請求不可重複領取。
- 停用作業不追回既有合法獎勵。
- 前端顯示值與後端實際發放值必須一致。

### 7.5 教材 AI 口說題庫

- 教材來源依「教材 → Unit／主題 → 頁碼範圍 → 核准文字」保存；第一版只有管理員可以把人工核對為 `reviewed` 的文字交給 AI 出題。
- AI 只根據核准文字產生問題、繁中提示、關鍵字、簡易回答、完整示範回答、延伸問題、可接受語意及發音／重音提示，不得用頁碼或書名猜測內容。
- AI 產生結果一律先為 `draft`；管理員逐題核對後才可 `published`。已發布題庫不可直接改寫，修正必須建立新版本。
- 學生自由回答時評估主題相關性與流暢度；只有切換至完整示範答案的跟讀模式，才以固定參考文字做逐字紅／黃／綠發音判定。
- 口說大挑戰的發音判定一律由後端依已發布 `question_id` 讀取完整示範答案；不得接受前端傳來的參考句。原始麥克風錄音只在瀏覽器暫存與回聽，伺服器只保存評分、辨識文字與逐字結果。每位學生 10 分鐘最多 12 次送評，避免惡意重送造成語音 API 成本失控。
- 原始課本 PDF／圖片只能放在私人 R2，不得透過公開網址或前端 Data API 直接讀取。單一 Unit 來源上限 20MB，支援 PDF、JPG、PNG、WebP；整本 PDF 上限 100MB／500 頁，由管理員瀏覽器在本機固定切成每 10 頁一批，每批不得超過 20MB，再以短效預簽網址直傳原檔及私人批次檔。後端必須重新驗證 MIME、大小、物件存在及 PDF signature 後才可 OCR。
- 整本教材 OCR 必須逐批保存 `uploaded`、`processing`、`review_required`、`completed` 或 `failed` 狀態；關閉頁面後保留已完成進度，單批失敗或處理超過十分鐘可重試，不得要求重跑整本教材。
- OCR 只建立 `draft`／`review_required` 文字，不得直接成為 AI 出題來源。管理員依每批頁碼校對並確認後才轉為 `reviewed`；整本所有批次核准後才算完成。OpenAI 暫存檔在每次辨識結束後刪除，私人 R2 原檔保留供稽核與重新校對。
- 題庫資料表啟用 RLS 並撤銷 `anon`／`authenticated` 直接權限；前端只能透過驗證 Firebase ID Token、重新核對角色的 Edge Function 操作。
- 已發布題目的完整示範回答使用後端 Google Cloud Text-to-Speech 的 Chirp 3 HD 語音產生，不再以瀏覽器 `speechSynthesis` 作為正式示範。只有管理員可要求生成；Google Cloud Service Account JSON 只存 Supabase Secret，不得傳到前端。文字、Voice ID、語言、取樣率及輸出格式組成固定雜湊，相同內容只生成一次；語音存私人 R2，Supabase 只保存資產狀態、雜湊與物件路徑，學生透過短效預簽網址播放。題目修改或聲音設定變更才建立新版資產，失敗可重試但不可讓學生直接呼叫付費 API。
- 畫面可保留 `[你的名字]` 等引導學生代換的提示，但 TTS 前必須轉成自然英文範例（例如 `My name is Amy.`），不得朗讀中文提示、括號或底線。Google 輸出使用該聲音的原生取樣率；朗讀清理規則版本屬於資產設定雜湊的一部分，規則更新後必須建立並連結新版資產，不得沿用或覆蓋舊檔。
- 國小生正式示範語音預設使用 Leda、`0.82` 倍語速及未壓縮 LINEAR16 WAV，避免原生速度過快與 MP3 壓縮造成的悶濁感。語速、編碼及處理版本必須納入設定雜湊；調整時建立新版資產並保留舊檔，不得覆寫既有語音。
- 學生錄音只在瀏覽器暫存供回聽；伺服器保存評分與完成狀態，不保存原始錄音。

## 8. 聽力播放與完成

### 8.1 有效聆聽

真正聆聽不重複音檔區段達 80% 才算一次有效完成。

- 只在實際播放時累積。
- 暫停時停止累積。
- 前端每段覆蓋率必須同時核對音檔播放時間與單調遞增的實際經過時間；瀏覽器延後觸發播放事件時，正常連續聆聽仍應累積，不得再用固定 3 秒事件間隔直接丟棄整段。
- 拖曳只改變播放位置，不增加覆蓋率。
- 重播同一小段不重複增加覆蓋率。
- 不能只依 `ended` 事件或目前時間除以音檔長度判定。
- 前端合併實際播放區段，後端再驗證不重複區段、播放時間、音檔長度與工作階段合理性。
- 首次播放時若音檔 metadata 尚未完成，必須在 `canplay` 或後續有效播放事件重試建立伺服器工作階段，不能讓第一首音檔只在前端計時。
- 頁面切到背景或裝置鎖定時必須暫停播放器；回到學習頁後由學生手動繼續，背景期間不計入覆蓋率。
- 未達 80% 的工作階段只保存診斷資料，不增加完成次數、XP、AE Points 或作業進度。
- 同一工作階段只能記錄一次有效播放。
- 只有 `role === "student"` 才寫入正式學習次數；老師與管理員試聽不計入學生統計。
- 既有 noInteraction 防自動掛機機制必須保留。

### 8.2 聽力 XP 與 AE Points（V3 已確認，尚未部署）

本節是已確認的新產品規則；在新的 additive migration、`record-play`／遊戲化後端與前端動畫完成部署前，不代表正式站現況已生效。

- 每次通過 80% 不重複區段與後端工作階段驗證，總播放次數就增加一次；學習統計不設 7／100 次門檻。
- 新版自主獎勵另用 `student_track_mastery` 累計：同音檔 10 次「未分配給作業」的有效聆聽，固定發 10 XP／1 AE Point，非機率抽獎。同學生＋音檔終身一次，來源鍵不得含日期。
- 每個台北日最多發 3 檔自主熟練獎勵（30 XP／3 AE Points）。達上限仍保存次數，滿 10/10 未發獎者保留待領，隔天再有效聽一次該檔才結算，不自動補發或歸零。
- 整份作業達老師指定條件後發 30 XP，一份一次；只有有效在校英文班學生另得 5 AE Points，不受自主每日 3 檔限制。升等 AE Points 也只限有效在校生。
- 畫面分開顯示「自主熟練度 6/10」、「老師指定 2/3，本次不另計自主獎勵」與「本音檔熟練獎勵已領取」。已領獎仍可練習，不能把「無新獎勵」顯示成沒有計次。
- 舊播放不回填新自主次數，舊獎勵不追回。既有作業進度、完成獎勵及升等紀錄保留。部署前已建立的 V2 session 依原規則完成，之後新建的 canary session 才用 V3；不更改既有灰度旗標、不擅自對所有學生啟用。
- 試用者、網購者、基本會員與離校生可以依有效學習行為累積 XP，但不新增 AE Points；既有合法點數與紀錄保留。停用或沒有有效聽力權限的帳號不得取得新獎勵。
- 教師與管理員播放不取得 XP、AE Points 或升等獎勵。

### 8.3 防掛機、重複請求與成本上限（已確認，待實作）

- 前端只負責回報播放區段與互動證據，Supabase 後端才是有效聆聽、每日額度、XP、AE Points 與升等的唯一權威。
- 拖曳、重播同一小段、暫停時間與加速播放不得計入可領獎覆蓋率；頁面隱藏或裝置鎖定時直接暫停。回來後持續顯示大型「播放已暫停」確認，說明背景不計次，點「我知道了，繼續播放」才續播；已暫停者不顯示，不因換檔或外部播放事件繞過。失敗保留重試、鍵盤焦點限制於確認視窗並支援 safe area。注意力確認優先，背景暫停不重設防掛機計數。
- 每個帳號同時間只能有一個可領獎的聽力工作階段；不同分頁或裝置同時播放不得疊加。
- 工作階段 ID、音檔、台北日期與獎勵來源鍵必須由後端以資料庫唯一條件及原子交易防止重送、併發與重複發獎；不能只靠前端旗標。
- 連續學習 15 分鐘或連續完成 5 個可領獎音檔後顯示「還在學習嗎？」；約 30 秒內未互動則暫停播放器，且目前工作階段不得領獎。此檢查不應每首都打斷正常學習。
- 需記錄重疊工作階段、異常精準的重複播放時間、連續大量無互動完成及無效／中斷工作階段等風險訊號。第一階段只停止該工作階段發獎並留下管理稽核，不自動封鎖帳號或刪除學習紀錄。
- 即使整天播放，自主熟練仍最多每日 30 XP／3 AE Points，且每檔終身限領。此上限不含老師作業（每份 30 XP／5 點）、升等及其他既有活動；不能將 3 點誤稱為全平台每日成本上限，作業發布頻率仍影響成本。
- 若教材已有題目或逐字稿，後續可加入低頻率理解題作為第二階段驗證，但不是第一階段上線的必要門檻。

### 8.4 播放器版面

- 播放器不可拖出畫面。
- 不遮住 Navbar、手機 Sidebar、今日作業或發布作業按鈕。
- Sidebar 的 z-index 高於播放器。
- 作業頁可以使用固定迷你播放器，但要保留 iPhone safe area。
- `900px`（含）以下使用緊湊播放器，優先保留播放／暫停、前後首、音檔名稱、有效聆聽率與音量；`901px` 以上才使用完整桌面排列，避免 `682px`～`810px` 被擠壓。

### 8.5 尚待完整驗證

加速播放已確認不得計入可領獎的有效聆聽；完成實作後仍需端到端驗證 80% 不重複區段、背景／鎖屏、互動確認、跨分頁 Session 與每日上限。不可只靠調整前端計時放寬後端防作弊。

## 9. 字幕、錯題複習與學習進度

### 9.1 字幕流程

建議學習順序：

1. 第一次播放不顯示字幕。
2. 第二次可顯示英文字幕。
3. 需要時顯示中文提示。
4. 最後可查看完整逐字稿。
5. 關閉字幕後重新練習。

音檔可包含 `transcript_en`、`transcript_zh`、`subtitle_cues`、`subtitle_status`。沒有可用字幕資料時，不顯示無作用的字幕按鈕。

### 9.2 智慧錯題複習

- 使用已存在的錯題紀錄與間隔複習排程。
- 不呼叫生成式 AI，也不消耗 AI 教材額度。
- 題目來源可以是過去的 AI 練習，但「複習」行為本身不是 AI 生成。
- 只要學生有 `features.review` 即可使用，不需要另外購買 AI 加購。
- 只能讀取自己的錯題與複習進度。

### 9.3 學習累積與報表

- 聽力、作業、複習、XP、AE Points 與兌換歷史必須保留。
- 會員或在學資格到期只鎖定新內容，不刪除歷史。
- 學生只看自己的資料；老師看授權班級；管理員可看全部。
- UI 必須區分「沒有紀錄」與「讀取失敗」，不能把錯誤當成 0。

## 10. XP、AE Points、排行榜與獎品商城

- XP 用於學習成長、等級與排行榜；AE Points 用於獎品兌換，兩者不能混用。
- 後端是獎勵計算與寫入的唯一權威；前端不得自行增加數值。
- 每次獎勵事件應有可防重複的工作階段或來源識別。
- 聽力依第 8 節規則發獎；混合作業整份完成固定為 30 XP，有效在校英文班學生另得 5 AE Points，不使用 AI 分數或發音高分加成。舊作業已發放的歷史獎勵保留。
- 所有有效學生可累積 XP；只有有效在校英文班學生可取得新的 AE Points、使用獎品商城及兌換。一般會員、網購者、試用者與離校生的既有點數不刪除，但資格不符時不能新增或兌換。
- 遊戲可保留勝負 XP，但所有遊戲合計每日最多 2 AE Points，避免以重複對戰大量取得可兌換點數。
- 升等獎勵只在第一次到達該等級時發放，來源鍵使用 `level:{level}` 並由後端保證冪等：Lv2–5 每級 5 AE Points、Lv6–10 每級 10 點、Lv11–20 每級 15 點、Lv21 以上每級 20 點。
- 升等畫面應顯示新等級、取得的 AE Points 與下一級進度；若一次增加多級，後端逐級發放一次性獎勵，前端合併成不冗長且可跳過的動畫。
- 學生可上傳自己的正方形頭像，或改用系統核准的五款預設頭像；預設路徑必須由後端白名單驗證，不能接受任意前端路徑。
- 已停用帳號不參與可操作的排行榜與兌換流程，但歷史資料保留供管理查詢。
- 獎品停用不刪除過去兌換紀錄。
- 兌換時必須由後端確認庫存、點數餘額、帳號狀態與重複請求。
- AE Points 的初始成本控制基準為每 NT$1 實際獎品成本約需 9–12 點；每位學生每 30 天最多兌換一次實體獎品，並可依庫存及學生設定個別限量。
- 建議初始兌換價格：小糖果／餅乾 180 點、無印良品筆 250 點、零食包 400 點、手搖飲或麥當勞 NT$50 電子券 600 點、無印良品筆記本 750 點、平價筆袋 1,200 點。
- 低價實體品以到班／門市領取為主，遠距會員優先提供電子券；食物獎品需考慮過敏、家長選擇、有效期限與無法供貨時的替代方案。

## 11. 帳號管理與學生建立

### 11.1 管理清單

管理員清單支援組合篩選：

- 姓名、登入帳號或 Email 搜尋
- Role
- Class
- Plan
- 開通狀態
- 帳號狀態
- 是否具有有效學習權限

預設只顯示使用中的真實帳號；停用帳號透過「已停用」篩選查找與恢復。手機版使用卡片呈現，不能讓桌面表格橫向溢出後把狀態與操作截斷。

### 11.2 安全停用與永久刪除

- 管理員可以永久刪除沒有付款、教材購買、加購或啟用碼兌換紀錄的學生測試帳號；教師與管理員帳號不得透過此功能刪除。
- 永久刪除必須依登入方式輸入完整 Email 或完整登入名稱再次確認，並同時清除 Firebase 登入身分及 Supabase 中的學習、作業、AI、獎勵與其他帳號關聯測試資料；此操作無法復原。
- Stripe 測試模式付款帳號可以刪除，但後端必須先以測試金鑰確認並取消測試訂閱、刪除測試 Customer，再清除測試付款資料；不得用正式金鑰執行此清理。
- 只要存在 Stripe 正式模式或無法確認模式的付款交易，或由教材購買、啟用碼產生的權限紀錄，後端必須拒絕永久刪除，帳號只能停用。
- 未領取且尚未完成開通的邀請可以刪除，但管理員也必須輸入完整 Email 再次確認。
- 一般離校、到期或不再使用的正式帳號仍採停用方式，保留學習與購買歷史。

### 11.3 邀請建立

- 本節是既有 Email 邀請帳號的相容流程；新建立的英文班在校生優先使用 11.4 的登入卡流程。
- 建立 72 小時有效、一次性邀請連結。
- 學生自行設定密碼並完成 Email 驗證。
- 系統不保存或記錄明文密碼。
- 邀請狀態與已建立帳號狀態要分開顯示。

### 11.4 快速建立與 CSV 批次建立

- 僅管理員可批次建立學生。
- 每批最多 25 位。
- 班級只允許 E1、E3、E5、E7。
- 中文姓名、英文姓名、班級與日期先在伺服器預覽驗證，再允許正式建立；英文姓名只允許英文字母及常見姓名標點。登入帳號可選填，未填時由系統產生唯一帳號。
- 家長 Email 是選填聯絡資料，可以由兄弟姊妹共用，不是學生登入識別，也不影響帳號啟用。
- 同批資料逐列回報成功或失敗；單列失敗不得中止整批。
- `request_id` 用於防止重複提交。
- Firebase 內部建立高熵啟動密碼，但不回傳、不顯示也不保存明文。
- 每位學生建立一個 30 天有效的一次性啟用連結與兩組一次性復原碼；資料庫只保存 SHA-256 雜湊。
- 管理員可立即列印 A4 登入卡；QR Code 必須在瀏覽器本機產生，不得將啟用連結交給第三方 QR 服務。
- 學生掃描登入卡後必須確認或補齊中文姓名與英文姓名，再自行設定至少 6 個字元的密碼；中英文姓名與密碼均由 Edge Function 再次驗證，不可信任前端 required。密碼可使用小寫英文、數字或符號，前端與 Edge Function 必須採用相同的最短長度規則。
- 密碼欄位預設不自動大寫、不啟用自動修正，並提供顯示／隱藏切換；介面需直接說明未達最短長度或兩次輸入不一致。
- 忘記密碼時先使用登入卡上的一次性復原碼；只有登入卡、裝置與復原碼皆遺失時才由老師或管理員重新發卡。
- 管理員對尚未啟用且使用中的英文班帳號重新發卡時，必須先撤銷舊 QR 啟用連結與所有未使用復原碼，再建立一張新的 30 天登入卡；不得重建學生帳號或刪除其資料。
- 啟用權杖與復原原碼只在成功結果顯示一次，可下載／列印交付，但不得寫入長期操作紀錄、Console 或 Git。
- 正式建立會同時處理 Firebase 身分與 Supabase 學生資料；任何部分失敗都必須明確回報，不能顯示假成功。
- 執行正式資料建立前仍須取得明確同意，並先以標記清楚的沙盒資料驗收。

### 11.5 驗證信與密碼重設信

- 非英文班帳號仍需使用真實、可收信 Email，並保留 Firebase Email 驗證與安全重設流程。
- Firebase Admin 只在 Edge Function 產生具有時效性的 action link，再由既有 Resend 寄件網域寄出 Alan English 品牌信件。
- 寄信 API 不得洩漏 Email 是否已註冊；重設密碼回應使用相同成功文案，並以雜湊識別做每小時節流。
- 寄件網域部署前必須確認 SPF、DKIM 與 DMARC；這些設定只能降低進垃圾郵件的機率，不能保證每封信都進主要收件匣。
- Email action link、Firebase Service Account 與 Resend 金鑰不得寫入資料庫長期紀錄、前端、Console 或 Git。

## 12. 教材、目錄與內容授權

- 教材列表可公開展示商品或導覽資訊，但完整內容依有效功能與教材 entitlement 判斷。
- 購書後的教材擁有權永久保留；附贈網站使用權從兌換／授權日開始精確計算 90 天，且不自動續費。
- 開通碼兌換只新增 grant，不覆蓋英文班資料、其他購買或既有進度。
- 班級教材、自購教材、管理員贈送、開通碼及試用來源必須疊加，任何來源不得覆蓋或刪除其他來源。
- 班級固定 E1、E3、E5、E7；班級教材採版本、生效日與 audit log，只有 admin 可修改，teacher 只讀自己授權班級。
- 新學期教材必須透過換版交易建立，不可直接覆寫舊版本。換版只能在生效當天執行，並在同一筆資料庫交易內盤點截至前一天的全部歷史教材證據、建立永久 `academy_history` entitlement、結束上一版及啟用新版；新入學學生只取得入學後實際適用的版本。既有作業維持發布時快照，新作業才採新版教材。
- 若管理員在換版完成後發現當天版本的學期名稱或教材選擇錯誤，只能透過「修正目前版本」交易調整當天建立且目前生效的同一版本，不得建立重疊的同日版本。修正時提交的是修正後完整教材清單；誤選教材從清單移除後，學生不再透過該班級版本取得，但不得因此撤銷學生由自行購買、管理員贈送、開通碼或真正歷史教材取得的其他 entitlement。修正可在當天重複執行，但每次都必須以最新 `updated_at` 重新預覽與二次確認，並留下 `corrected` 前後快照；不得刪除學習紀錄或破壞既有作業快照。隔日後不得回寫歷史版本，應依當日版本流程留下新的時間邊界。
- 轉班只結束舊班級來源並採用新班級設定；自購／贈送教材與歷史進度保留。離校生效後班級來源結束，但登入、自購／贈送教材、XP、AE Points、等級及歷史紀錄保留。
- `requires_book_entitlement = true` 時，即使有聽力功能，也只能打開被授權的教材。
- 教材、音檔、字幕、逐字稿、學習紀錄與 R2 播放 URL 都要由 Firebase 驗證的 Edge Function 重新檢查有效期間與教材來源；不得相信前端角色、student_id、learner_type、班級、教材 ID 或音檔 ID。
- 逐字稿只對有效教材權限提供。第一次播放預設隱藏，學生可依序開啟英文字幕、中文提示與完整逐字稿，並可關閉再次練習；沒有逐字稿時不顯示按鈕。

### 12.1 作業教材快照

- 老師必須先選自己獲授權的固定班級，教材只能來自該班目前生效設定，音檔只能屬於所選教材；空白班級與跨班音檔一律拒絕。
- `assignment-manager` 必須重新查詢老師角色、授權班級、目標班級、班級教材、音檔所屬教材與發布者權限。
- 發布時保存班級教材設定、教材及音檔快照。之後修改班級教材不得破壞既有未到期作業，新作業才使用新版本。

## 13. 付款與方案狀態

- Stripe Checkout、Customer Portal 與 Webhook 只新增或更新對應的 access grant，不直接覆蓋其他權限來源。
- Webhook 是付款狀態的後端權威；前端成功頁不能自行開通權限。
- Checkout 前若缺少有效家長 Email 必須阻止付款並導向學生設定。
- 月費只有家長明確完成 Checkout 後才自動續訂；支援 Customer Portal、本期結束取消、到期前恢復。真正到期後重新訂閱必須開始新週期，不追溯計費。
- 離校不得讓已付款方案立即失效；基本月費與 AI 均使用到 `current_period_end`，並於本期結束取消。到期前恢復在校可撤銷取消排程。
- 成功付款、續費、付款失敗、取消與到期要映射成清楚且可稽核的 grant 狀態。
- 通知事件必須有 durable event key 防止重複，並分別建立學生通知及家長 Email queue；教材 90 天、排程取消／到期、預定離校及付款失敗在到期前三天最多建立一次。
- TWD 金額與 Stripe 最小金額單位需正確換算，例如 NT$499 對應 `49900`。
- 任何付款識別碼或 Secret 不回傳前端、不寫入公開文件。
- 目前只允許 Stripe test mode；切換 live mode 必須另行核准並重新做完整驗收。

## 14. UI、資訊密度與 RWD

- 所有具有主要 Navbar／Header 的頁面，導覽列必須固定在瀏覽器畫面頂部，頁面捲動時不可跟著離開畫面；內容需保留對應頂部空間，並考慮 iPhone safe area，不能被固定導覽列遮住。
- 手機版優先，主要驗收寬度約 412px，並考慮 iPhone Safari safe area。
- 互動按鈕觸控範圍至少約 44px。
- 字重以可讀為優先，避免大量過粗文字；重要數字可放大，但不能靠所有文字加粗製造層級。
- Dashboard 資訊較多時，合併重複欄位、放大關鍵進度，移除未啟用或純測試佔位內容。
- 學習累積卡應讓圖示、標籤與數值形成清楚群組，避免圖示旁留過多無意義空白。
- 作業進度如 `(1/4)`、剩餘題數或任務數要比說明文字更容易辨識。
- 已停用帳號用紅色系清楚標示，但仍須維持文字對比與可讀性。
- 手機表格應改為卡片或可理解的響應式結構，不可只靠水平捲動隱藏重要欄位。
- 學生桌面 Navbar 只保留直接開始學習的入口：首頁、我的教材、發音教練、口說大挑戰與 AI 教材；「英文對話」暫時隱藏，但不刪除路由與既有資料。方案與功能仍提供家長／學生確認身分、期限與權限，但移至桌面帳號選單及手機「其他」區，不占用主要學習列；等級、排行榜、獎品及其他次要功能集中在「全部功能」Sidebar。
- Sidebar 開啟時鎖定背景捲動，且可用 X、遮罩或返回操作關閉。

## 15. 錯誤、快取與狀態顯示

- 未授權、方案到期、帳號停用、資料不存在與伺服器錯誤必須使用不同錯誤狀態。
- 401／403 不得被畫面當成空資料或 0 次紀錄。
- 登入後的會員與有效權限可以快取，但帳號狀態變更、付款 Webhook 生效、登出或 `ACCOUNT_ARCHIVED` 時要失效。
- 同一頁同時載入 Session 與學生資料時應共用請求，避免成功結果被較慢的失敗請求覆蓋而登出。
- 錯誤訊息不能包含 Token、Secret、明文密碼或未遮蔽個資。

## 16. 修改邏輯時的同步檢查

調整產品規則時，至少檢查以下層級是否一致：

1. 公開說明與價格文字。
2. Navbar、Sidebar、Dashboard 卡片及直接網址進入時的顯示。
3. React 頁面內的功能條件。
4. `effective_access` 或資料庫方案／grant 計算。
5. Edge Function 的讀取與寫入授權。
6. Migration、RLS、索引與稽核資料。
7. 桌面、412px 手機與 iPhone safe area。
8. 角色、學生類型、停用帳號、到期方案與直接 API 請求測試。

任何新增功能都不應只靠「把按鈕藏起來」完成權限控制。

## 17. 目前需持續追蹤的邏輯缺口

- 統一七天試用 AI 額度的文字規範：舊規範每日 5 次，現行實作每日 2 次且七天總共 7 次。
- 完成老師作業的 30 XP／5 AE Points 需核對所有作業類型的後端一次性發獎流程。
- 使用真實 412px 裝置／預覽環境驗收帳號卡片、停用篩選、恢復操作與浮動按鈕遮擋。
- 用明確沙盒學生驗收 CSV 正式建立、重複 Email、部分失敗、重複 request ID 與一次性結果下載。
- 完成 MusicPlayer 80% 不重複區段、拖曳、重播、背景播放與加速播放的端到端測試。
- 確認每個直接路由除了角色保護外，頁內功能與後端都使用相同 `features`／教材 entitlement。

## 18. 主要實作位置

以下路徑是本文件邏輯的主要入口，不代表完整檔案清單：

- 路由與角色：`src/app/App.jsx`、`src/auth/ProtectedRoute.jsx`、`src/auth/AuthContext.jsx`
- 有效權限：`supabase/functions/_shared/effective-access.ts`
- 會員與帳號：`supabase/functions/membership-manager/index.ts`
- AI 教材：`src/components/Pages/AIMaterialGenerator.jsx`、`supabase/functions/generate-ai-material/index.ts`
- 作業：`src/components/Pages/TeacherAssignments.jsx`、`src/components/Pages/StudentAssignments.jsx`、`supabase/functions/assignment-manager/index.ts`
- 聽力：`src/components/fragment/MusicPlayer.jsx`、`supabase/functions/record-play/index.ts`
- 帳號管理：`src/components/Pages/AccountManagement.jsx`
- CSV 建立：`src/components/Pages/AcademyStudentCsvImport.jsx`、`supabase/functions/academy-student-manager/index.ts`
- 付款：`supabase/functions/billing-manager/index.ts`、`supabase/functions/stripe-webhook/index.ts`
- 獎勵：`supabase/functions/gamification/index.ts`

每次產品邏輯變更完成後，應小幅更新本文件對應章節，並將實際進度、測試、commit、PR 與部署狀態寫入 `docs/PROJECT_STATUS.md`。
